# Web AR Tools Monorepo

A WebXR-first AR toolkit for browser and headset browsers.

## Quick start

```bash
pnpm install
pnpm build
```

## Structure

- `apps/studio-web` main AR app
- `packages/*` publishable toolkit packages
- `services/*` optional backend services
- `planning/masterplan.yaml` execution roadmap
